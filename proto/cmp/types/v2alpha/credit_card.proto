syntax = "proto3";

package cmp.types.v2alpha;

import "cmp/types/v2alpha/date.proto";

message CreditCard {
  // FIXME: Can we use an int64 to represent a credit card number?
  string number = 1;
  Date expiration_date = 2;
  int32 cvc = 3;
}
